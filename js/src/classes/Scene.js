var Game = Game || {};

Game.Scene = function(config) {
	var _self = this;

	var _init = function(config) {
		// create actors?
	};

	// _self.run = function(scene){ // switch statement? }


	// Example scenes:
	// explosion (small, medium, large)
	// Chain of grouped explosions
	// Laser burn
	// Terrain crumbling (create 4 terrain pieces with expiration times)

	_init(config);
};
